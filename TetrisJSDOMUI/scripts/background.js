var paper = Raphael(650, 0, 600, 600),
    size = 50;

paper.rect(0, size, size, size).attr({
    stroke: 'black',
    fill: '#00ff00'
});

paper.rect(size, size, size, size).attr({
    stroke: 'black',
    fill: '#00ff00'
});

paper.rect(0, 2 * size, size, size).attr({
    stroke: 'black',
    fill: '#00ff00'
});

paper.rect(size, 2 * size, size, size).attr({
    stroke: 'black',
    fill: '#00ff00'
});

paper.rect(4 * size, 2 * size, size, size).attr({
    stroke: 'black',
    fill: 'skyblue'
});

paper.rect(4 * size, 3 * size, size, size).attr({
    stroke: 'black',
    fill: 'skyblue'
});

paper.rect(5 * size, 3 * size, size, size).attr({
    stroke: 'black',
    fill: 'skyblue'
});

paper.rect(6 * size, 3 * size, size, size).attr({
    stroke: 'black',
    fill: 'skyblue'
});

paper.rect(0, 5 * size, size, size).attr({
    stroke: 'black',
    fill: '#F05028'
});

paper.rect(size, 4 * size, size, size).attr({
    stroke: 'black',
    fill: '#F05028'
});

paper.rect(size, 5 * size, size, size).attr({
    stroke: 'black',
    fill: '#F05028'
});

paper.rect(0, 6 * size, size, size).attr({
    stroke: 'black',
    fill: '#F05028'
});

paper.rect(9 * size, size, size, size).attr({
    stroke: 'black',
    fill: '#b535c6'
});

paper.rect(9 * size, 2 * size, size, size).attr({
    stroke: 'black',
    fill: '#b535c6'
});

paper.rect(8 * size, 2 * size, size, size).attr({
    stroke: 'black',
    fill: '#b535c6'
});

paper.rect(9 * size, 3 * size, size, size).attr({
    stroke: 'black',
    fill: '#b535c6'
});

paper.rect(11 * size, size, size, size).attr({
    stroke: 'black',
    fill: '#F8C94E'
});

paper.rect(11 * size, 2 * size, size, size).attr({
    stroke: 'black',
    fill: '#F8C94E'
});

paper.rect(11 * size, 3 * size, size, size).attr({
    stroke: 'black',
    fill: '#F8C94E'
});

paper.rect(11 * size, 4 * size, size, size).attr({
    stroke: 'black',
    fill: '#F8C94E'
});

paper.rect(4 * size, 6 * size, size, size).attr({
    stroke: 'black',
    fill: '#0949ea'
});

paper.rect(3 * size, 7 * size, size, size).attr({
    stroke: 'black',
    fill: '#0949ea'
});

paper.rect(2 * size, 7 * size, size, size).attr({
    stroke: 'black',
    fill: '#0949ea'
});

paper.rect(4 * size, 7 * size, size, size).attr({
    stroke: 'black',
    fill: '#0949ea'
});

paper.rect(6 * size, 8 * size, size, size).attr({
    stroke: 'black',
    fill: '#f74f79'
});

paper.rect(6 * size, 9 * size, size, size).attr({
    stroke: 'black',
    fill: '#f74f79'
});

paper.rect(7 * size, 9 * size, size, size).attr({
    stroke: 'black',
    fill: '#f74f79'
});

paper.rect(7 * size, 10 * size, size, size).attr({
    stroke: 'black',
    fill: '#f74f79'
});

paper.rect(0, 10 * size, size, size).attr({
    stroke: 'black',
    fill: '#f4071b'
});

paper.rect(size, 10 * size, size, size).attr({
    stroke: 'black',
    fill: '#f4071b'
});

paper.rect(2 * size, 10 * size, size, size).attr({
    stroke: 'black',
    fill: '#f4071b'
});

paper.rect(3 * size, 10 * size, size, size).attr({
    stroke: 'black',
    fill: '#f4071b'
});

paper.rect(10 * size, 10 * size, size, size).attr({
    stroke: 'black',
    fill: 'green'
});

paper.rect(11 * size, 10 * size, size, size).attr({
    stroke: 'black',
    fill: 'green'
});

paper.rect(11 * size, 9 * size, size, size).attr({
    stroke: 'black',
    fill: 'green'
});

paper.rect(11 * size, 8 * size, size, size).attr({
    stroke: 'black',
    fill: 'green'
});

paper.rect(8 * size, 6 * size, size, size).attr({
    stroke: 'black',
    fill: 'yellow'
});

paper.rect(9 * size, 6 * size, size, size).attr({
    stroke: 'black',
    fill: 'yellow'
});

paper.rect(8 * size, 7 * size, size, size).attr({
    stroke: 'black',
    fill: 'yellow'
});

paper.rect(9 * size, 7 * size, size, size).attr({
    stroke: 'black',
    fill: 'yellow'
});